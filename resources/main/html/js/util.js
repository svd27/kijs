function KIDATAset(node, name, value) {
  node.dataset[name] = value
}

function KIDATAget(node, name) {
  return node.dataset[name]
}

function KIStyle(node, name) {
  return node.style[name]
}

function KIStyle(node, name, value) {
  node.style[name] = value
}